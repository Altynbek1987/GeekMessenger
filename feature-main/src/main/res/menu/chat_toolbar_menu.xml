<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/btn_overflow_menu"
        android:icon="@drawable/ic_overflow_menu"
        android:title="OverflowMenu"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/btn_clear_chat"
                android:icon="@drawable/ic_clear_chat"
                android:orderInCategory="2"
                android:title="@string/clear_chat_russian"
                app:showAsAction="never" />

            <item
                android:id="@+id/btn_mute"
                android:icon="@drawable/ic_mute_notifications"
                android:orderInCategory="3"
                android:title="@string/mute_russian"
                app:showAsAction="never" />
        </menu>
    </item>
</menu>